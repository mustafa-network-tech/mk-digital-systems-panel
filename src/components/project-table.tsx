"use client";import {useState} from "react";import Link from "next/link";import {projects} from "@/lib/data";import {Status,Health} from "./status";import {Search,Star,MoreHorizontal} from "lucide-react";
export function ProjectTable(){const [q,setQ]=useState("");const [filter,setFilter]=useState("tum");const items=projects.filter(p=>(filter==="tum"||p.status===filter)&&(`${p.name} ${p.customer}`.toLowerCase().includes(q.toLowerCase())));return <div className="card overflow-hidden"><div className="flex flex-col gap-3 border-b p-4 md:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg border px-3"><Search size={17} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Proje veya müşteri ara..." className="w-full py-2.5 outline-none"/></label><select className="field md:w-48" value={filter} onChange={e=>setFilter(e.target.value)}><option value="tum">Tüm durumlar</option><option value="aktif">Aktif</option><option value="gelistiriliyor">Geliştiriliyor</option><option value="test">Test</option><option value="beklemede">Beklemede</option></select></div><div className="overflow-x-auto"><table><thead><tr><th>Proje</th><th>Müşteri</th><th>Durum</th><th>Sağlık</th><th>Son yedek</th><th></th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td><div className="flex items-center gap-3"><button aria-label="Favori"><Star size={16} fill={p.favorite?"#eab308":"none"} color={p.favorite?"#eab308":"#9ca3af"}/></button><div><Link href={`/projects/${p.id}`} className="font-semibold hover:text-blue-600">{p.name}</Link><div className="mt-1 text-xs muted">{p.short}</div></div></div></td><td className="text-sm">{p.customer}</td><td><Status value={p.status}/></td><td><Health value={p.health}/></td><td className={`text-sm ${p.backup.includes("36")||p.backup.includes("yok")?"text-red-600":"muted"}`}>{p.backup}</td><td><MoreHorizontal size={18}/></td></tr>)}</tbody></table>{!items.length&&<div className="p-12 text-center muted">Aramanızla eşleşen proje bulunamadı.</div>}</div></div>}

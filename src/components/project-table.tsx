"use client";import {useState} from "react";import Link from "next/link";import type {Project} from "@/lib/types";import {Status,Health} from "./status";import {Search,Star} from "lucide-react";
export function ProjectTable({projects,emptyText="Henüz proje bulunmuyor."}:{projects:Project[];emptyText?:string}){const [q,setQ]=useState(""),[filter,setFilter]=useState("tum");const items=projects.filter(p=>(filter==="tum"||p.status===filter)&&(`${p.name} ${p.customers?.name||"MK Digital Systems"}`.toLocaleLowerCase("tr").includes(q.toLocaleLowerCase("tr"))));return <div className="card overflow-hidden"><div className="flex flex-col gap-3 border-b p-4 md:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg border px-3"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Proje veya müşteri ara..." className="w-full py-2.5 outline-none"/></label><select className="field md:w-48" value={filter} onChange={e=>setFilter(e.target.value)}><option value="tum">Tüm durumlar</option><option value="aktif">Aktif</option><option value="gelistiriliyor">Geliştiriliyor</option><option value="test">Test</option><option value="tamamlandi">Tamamlandı</option></select></div><div className="overflow-x-auto"><table><thead><tr><th>Proje</th><th>Proje Sahibi</th><th>Durum</th><th>Sağlık</th><th>Son yedek</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td><div className="flex items-center gap-3"><Star size={16} fill={p.favorite?"#eab308":"none"}/><Link href={`/projects/${p.id}`} className="font-semibold hover:text-blue-600">{p.name}</Link></div></td><td>{p.customers?.name||"MK Digital Systems"}</td><td><Status value={p.status}/></td><td><Health value={p.health_status}/></td><td className="text-sm muted">{p.last_backup_at?new Date(p.last_backup_at).toLocaleDateString("tr-TR"):"Yedek yok"}</td></tr>)}</tbody></table>{!items.length&&<div className="p-12 text-center muted">{emptyText}</div>}</div></div>}

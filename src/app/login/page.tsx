"use client";import {useState} from "react";import {ShieldCheck,Loader2} from "lucide-react";import {createBrowserClient} from "@supabase/ssr";import {toast} from "sonner";import {useRouter} from "next/navigation";
export default function Login(){const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const fd=new FormData(e.currentTarget),url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){toast.error("Supabase ortam değişkenleri yapılandırılmamış");setLoading(false);return}const supabase=createBrowserClient(url,key);const {error}=await supabase.auth.signInWithPassword({email:String(fd.get("email")),password:String(fd.get("password"))});if(error){toast.error("E-posta veya şifre hatalı");setLoading(false);return}toast.success("Giriş başarılı");router.push("/dashboard");router.refresh()}return <main className="grid min-h-screen place-items-center bg-[#10131a] p-5"><div className="w-full max-w-md rounded-2xl bg-white p-8 shadow-2xl"><div className="mb-8 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-blue-600 text-white"><ShieldCheck/></div><div><h1 className="font-bold">MK Digital Vault</h1><p className="text-xs muted">MK DIGITAL SYSTEMS</p></div></div><h2 className="text-2xl font-bold">Tekrar hoş geldiniz</h2><p className="mt-2 text-sm muted">Güvenli proje kasanıza giriş yapın.</p><form className="mt-7 space-y-4" onSubmit={submit}><label className="block text-sm font-medium">E-posta<input required type="email" name="email" className="field mt-2" placeholder="admin@mkdigital.systems"/></label><label className="block text-sm font-medium">Şifre<input required minLength={6} type="password" name="password" className="field mt-2" placeholder="••••••••"/></label><button disabled={loading} className="btn btn-primary w-full justify-center">{loading&&<Loader2 className="animate-spin" size={17}/>}Giriş yap</button></form><p className="mt-8 text-center text-xs muted">Yetkisiz erişim girişimleri kayıt altına alınır.</p></div></main>}

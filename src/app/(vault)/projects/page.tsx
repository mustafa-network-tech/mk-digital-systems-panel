import {Header} from "@/components/sidebar";import {ProjectTable} from "@/components/project-table";import {createClient} from "@/lib/supabase/server";import type {Project} from "@/lib/types";import Link from "next/link";import {Plus} from "lucide-react";export default async function Page(){const supabase=await createClient();const {data,error}=await supabase.from("projects").select("*,customers(name)").eq("archived",false).order("updated_at",{ascending:false});if(error)throw new Error(error.message);return <><Header title="Projeler" description="Tüm yazılım projelerini ve teknik varlıklarını yönetin."/><div className="p-5 lg:p-8"><div className="mb-5 flex justify-end"><Link href="/projects/new" className="btn btn-primary"><Plus size={17}/>Yeni Proje</Link></div><ProjectTable projects={(data||[]) as Project[]}/></div></>}

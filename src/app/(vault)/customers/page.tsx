import {Header} from "@/components/sidebar";import {createClient} from "@/lib/supabase/server";import {Building2} from "lucide-react";export default async function Page(){const supabase=await createClient();const {data,error}=await supabase.from("customers").select("*,projects(count)").order("name");if(error)throw new Error(error.message);return <><Header title="Müşteriler" description="Müşterileri ve ilişkili projeleri yönetin."/><div className="p-5 lg:p-8"><div className="card overflow-x-auto"><table><thead><tr><th>Müşteri</th><th>Yetkili</th><th>İletişim</th><th>Projeler</th></tr></thead><tbody>{(data||[]).map(c=><tr key={c.id}><td><div className="flex items-center gap-3"><Building2 size={18}/><b>{c.name}</b></div></td><td>{c.contact_person||"—"}</td><td><div>{c.email||"—"}</div><div className="text-xs muted">{c.phone}</div></td><td>{c.projects?.[0]?.count||0}</td></tr>)}</tbody></table>{!data?.length&&<div className="p-12 text-center muted">Henüz müşteri bulunmuyor.</div>}</div></div></>}

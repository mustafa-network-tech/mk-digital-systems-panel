import {Header} from "@/components/sidebar";import {getCash} from "@/lib/finance";import {MonthPicker,FinanceNav,CashSummary,DonutChart} from "@/components/finance";import {adjustCashBalance,transferCash,moveCash} from "@/app/actions";import {money} from "@/lib/utils";
export default async function Page({searchParams}:{searchParams:Promise<{month?:string}>}){const {month}=await searchParams,{range,accounts,movements}=await getCash(month);const today=new Date().toISOString().slice(0,10);return <><Header title="Kasa" description="Mevcut kullanılabilir para ve hesap hareketleri."/><div className="space-y-6 p-5 lg:p-8"><div className="flex flex-wrap justify-between gap-4"><FinanceNav/><MonthPicker value={range.value} path="/finance/cash"/></div><CashSummary accounts={accounts}/><section className="card"><h2 className="border-b p-5 font-bold">Hesap Dağılımı</h2><DonutChart items={accounts.map(x=>({name:x.name,value:Number(x.balance)}))} emptyText="Kasa bakiyesi bulunmuyor."/></section><div className="grid gap-5 xl:grid-cols-3"><section className="card p-5"><h2 className="mb-4 font-bold">Bakiye Güncelle</h2><form action={adjustCashBalance} className="space-y-3"><Select name="account_id" accounts={accounts}/><input required name="balance" type="number" min="0" step="0.01" className="field" placeholder="Yeni bakiye"/><input name="date" type="date" defaultValue={today} className="field"/><input name="description" className="field" placeholder="Açıklama"/><button className="btn btn-primary">Bakiyeyi güncelle</button></form></section><section className="card p-5"><h2 className="mb-4 font-bold">Para Girişi / Çıkışı</h2><form action={moveCash} className="space-y-3"><Select name="account_id" accounts={accounts}/><select name="type" className="field"><option value="income">Para girişi</option><option value="expense">Para çıkışı</option></select><input required name="amount" type="number" min="0.01" step="0.01" className="field" placeholder="Tutar"/><input name="date" type="date" defaultValue={today} className="field"/><input name="description" className="field" placeholder="Açıklama"/><button className="btn btn-primary">Hareketi kaydet</button></form></section><section className="card p-5"><h2 className="mb-4 font-bold">Hesaplar Arası Transfer</h2><form action={transferCash} className="space-y-3"><Select name="from" accounts={accounts} placeholder="Kaynak hesap"/><Select name="to" accounts={accounts} placeholder="Hedef hesap"/><input required name="amount" type="number" min="0.01" step="0.01" className="field" placeholder="Tutar"/><input name="date" type="date" defaultValue={today} className="field"/><input name="description" className="field" placeholder="Açıklama"/><button className="btn btn-primary">Transfer et</button></form></section></div><section className="card overflow-x-auto"><h2 className="border-b p-5 font-bold">Kasa Hareket Geçmişi</h2><table><thead><tr><th>Tarih</th><th>Hesap</th><th>İşlem türü</th><th>Tutar</th><th>Açıklama</th><th>Oluşturulma</th></tr></thead><tbody>{movements.map(x=><tr key={x.id}><td>{new Date(x.movement_date).toLocaleDateString("tr-TR")}</td><td>{x.finance_accounts?.name||"—"}</td><td>{({income:"Para girişi",expense:"Para çıkışı",transfer:"Hesaplar arası transfer",adjustment:"Manuel bakiye düzeltmesi"} as const)[x.movement_type]}</td><td className="font-bold">{money(Number(x.amount))}</td><td>{x.description||"—"}</td><td className="text-sm muted">{new Date(x.created_at).toLocaleString("tr-TR")}</td></tr>)}</tbody></table>{!movements.length&&<div className="p-10 text-center text-sm muted">Seçilen ayda kasa hareketi bulunmuyor.</div>}</section></div></>}
function Select({name,accounts,placeholder="Hesap seçin"}:{name:string;accounts:{id:string;name:string}[];placeholder?:string}){return <select required name={name} className="field"><option value="">{placeholder}</option>{accounts.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select>}

import Link from "next/link";import {Header} from "@/components/sidebar";import {createClient} from "@/lib/supabase/server";import type {Domain} from "@/lib/types";
export default async function Page(){const s=await createClient(),{data,error}=await s.from("domains").select("*,projects(name)").order("expiry_date");if(error)throw new Error(error.message);return <><Header title="Domainler" description="Domain sürelerini ve registrar bilgilerini yönetin."/><div className="p-5 lg:p-8"><div className="card overflow-x-auto"><table><thead><tr><th>Domain</th><th>Proje</th><th>Registrar</th><th>Bitiş tarihi</th><th></th></tr></thead><tbody>{((data||[]) as Domain[]).map(d=><tr key={d.id}><td className="font-semibold">{d.domain}</td><td>{d.projects?.name||"—"}</td><td>{d.registrar||"—"}</td><td>{d.expiry_date?new Date(d.expiry_date).toLocaleDateString("tr-TR"):"—"}</td><td><Link className="btn" href={`/domains/${d.id}/edit`}>Düzenle</Link></td></tr>)}</tbody></table>{!data?.length&&<div className="p-12 text-center muted">Henüz domain bulunmuyor. Domainler proje detayından eklenir.</div>}</div></div></>}
